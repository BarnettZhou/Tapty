{
  "name": "tapty",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @tapty/server start"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}